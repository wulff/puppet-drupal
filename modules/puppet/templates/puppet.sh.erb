#!/bin/bash

PATH=/usr/local/bin:/usr/bin:/bin

apt-get update

puppet \
  --verbose \
  --logdest=/var/log/puppet.log \
  --modulepath=/home/<%= username %>/puppet-drupal/modules/ \
  /home/<%= username %>/puppet-drupal/manifests/drupal-host.pp 
